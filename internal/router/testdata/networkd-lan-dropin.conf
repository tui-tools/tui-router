# Written by tui-network. The DHCP screen owns this file in full:
# the pool, the lease time, the advertised options and the
# static leases of the DHCP server on lan0.
# systemd-networkd re-reads it on `networkctl reload`.

[DHCPServer]
PoolOffset=50
PoolSize=20
DefaultLeaseTimeSec=12h
DNS=
EmitDNS=yes
DNS=192.0.2.1

[DHCPServerStaticLease]
MACAddress=00:00:5e:00:53:31
Address=192.0.2.60
